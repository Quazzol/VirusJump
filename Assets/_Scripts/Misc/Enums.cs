namespace Misc
{
    public enum CellState
    {
        Normal,
        Infecting,
        Infected,
        Blown
    }

    public enum GameState
    {
        Ready,
        Playing,
        Stopped
    }
}